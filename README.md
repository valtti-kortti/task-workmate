# Task Workmate API

Асинхронный HTTP-сервис на Go, который управляет задачами, моделируя I/O-bound операции с длительным временем выполнения (3–5 минут).

## ⚡️ Возможности

- ✍️ Создание задач
- ❌ Удаление задач
- ℹ️ Получение статуса, времени создания и длительности обработки

## 💡 Принцип работы

Каждая задача запускается в фоне и выполняется "долго" (имитация через `time.Sleep`). Все данные хранятся в оперативной памяти.

## ⚙️ Запуск

### 1. Установить зависимости

```bash
go mod tidy
```

### 2. Создать `.env` файл

```env
PORT=":8080"
```

### 3. Запустить сервер

```bash
go run main.go
```

Сервер будет доступен на `http://localhost:8080`

---

## 🔗 API Эндпоинты

### ✍️ POST /tasks/

Создать новую задачу

#### Пример запроса:

```json
{
  "title": "example task"
}
```

#### Пример ответа:

```json
{
  "id": 1,
  "title": "example task",
  "status": "in_progress",
  "created_at": "2025-06-28T04:20:00Z",
  "process_time": 0
}
```

---

### 🔢 GET /tasks/\:id

Получить статус задачи по ID

#### Пример ответа:

```json
{
  "id": 1,
  "title": "example task",
  "status": "done",
  "created_at": "2025-06-28T04:20:00Z",
  "process_time": 180
}
```

---

### ❌ DELETE /tasks/\:id

Удалить задачу

#### Пример ответа:

```json
{
  "message": "task deleted"
}
```

---

## 🛡️ Технические детали

- Go 1.20+
- Fiber Web Framework
- Обработка задач происходит в фоне (через `go func`) без блокировки HTTP


